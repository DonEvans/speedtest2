<% provide(:title, 'Results' ) %>
<% @computer = @computer.sort { |a,b| a.time <=> b.time } %>

<h3>Systems Tested</h3>

<p> <%= (@computer.first).userName %> submitted the fastest system, a <%= (@computer.first).category %>, which needed <%=  (@computer.first).time %> ms. &nbsp; 
<%= link_to "show", :action => 'show', :id => @computer.first.id %>
<br>
<%= (@computer.last).userName %> submitted the slowest system, a <%= (@computer.last).category %>, which needed <%=  (@computer.last).time %> ms. &nbsp; 
<%= link_to "show", :action => 'show', :id => @computer.last.id %>
</p>

<h4>Desktops</h4>
<table>
  <tr>
    <td>Time in milliseconds </td> <td>&nbsp; | Browser |</td> 
    <td class="description" >Machine Description</td> <td>&nbsp;</td>
  </tr>
<% for computers in @computer %>
  <% if computers.category == "Desktop" %>
    <tr>
    <td>
      <div style="background-color: #69C; width:<%=
      (computers.time / 10)  %>px; max-width: 400px;">
      <%= computers.time %>
      </div>
    </td>
    <td>
      <%= computers.browser %>
    </td>
    <td class="description" >
      <%=  computers.description %>
    </td>
    <td>
      <%= link_to "show", :action => 'show', :id => computers %>
    </td>
    </tr>
  <% end %>
<% end %>
</table>

<h4>Mobile Devices</h4>
<table>
  <tr>
    <td>Time in milliseconds </td> <td>&nbsp; | Browser |</td>
<td class="description" >Machine Description</td>
  </tr>
<% for computers in @computer %>
  <% if (computers.category == "Mobile")  %>
    <tr>
    <td>
      <div style="background-color: #69C; width:<%=
      (computers.time / 10)  %>px; max-width: 400px;">
      <%= computers.time %>
      </div>
    </td>
    <td>
      <%= computers.browser %>
    </td>
    <td class="description" >
      <%=  computers.description %>
    </td>
    <td>
      <%= link_to "show", :action => 'show', :id => computers %>
    </td>
    </tr>
  <% end %>
<% end %>
</table>

<h4>Other</h4>
<table>
  <tr>
    <td>Time in milliseconds </td> <td>&nbsp; | Browser |</td>
<td class="description" >Machine Description</td>
  </tr>
<% for computers in @computer %>
  <% if computers.category == "Other" %>
    <tr>
    <td>
      <div style="background-color: #69C; width:<%=
      (computers.time / 10)  %>px; max-width: 400px;">
      <%= computers.time %>
      </div>
    </td>
    <td>
      <%= computers.browser %>
    </td>
    <td class="description" >
      <%=  computers.description %>
    </td>
    <td>
      <%= link_to "show", :action => 'show', :id => computers %>
    </td>
    </tr>
  <% end %>
<% end %>
</table>
